version: '3.8'

services:
  db:
    image: mysql:8
    container_name: proyecto_bd
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: proyecto
    ports:
      - "3306:3306"
    volumes:
      - ./db:/docker-entrypoint-initdb.d
